server {
    listen 9001;
    listen [::]:9001;
    server_name _;

    root /opt/cof-java/cof-web-vue3/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:9002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws/ {
        proxy_pass http://127.0.0.1:9002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }

    # Vue routes: /cards, /cards/loading, /cards/info, /cards/submit
    location ~ ^/cards(/(loading|info|submit))?$ {
        try_files /index.html =404;
    }

    # Static card images (numeric deck paths, lib/, *.png|jpg, etc.)
    location /cards/ {
        try_files $uri @cards_backend;
    }
    location @cards_backend {
        proxy_pass http://127.0.0.1:9002;
        proxy_set_header Host $host;
    }

    # Vite bundles under /assets/*.js; game images fall through to backend
    location /assets/ {
        try_files $uri @cof_assets_backend;
    }
    location @cof_assets_backend {
        proxy_pass http://127.0.0.1:9002;
        proxy_set_header Host $host;
    }

    location /audio/ {
        proxy_pass http://127.0.0.1:9002;
        proxy_set_header Host $host;
    }
}
